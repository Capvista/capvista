name: Deploy Web

on:
  push:
    branches: [main]
    paths:
      - 'apps/web/**'
      - 'packages/database/**'
      - '.github/workflows/deploy-web.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Azure Container Registry
        uses: azure/docker-login@v1
        with:
          login-server: capvistaregistry.azurecr.io
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and push Docker image
        run: |
          docker build -f apps/web/Dockerfile \
            --build-arg NEXT_PUBLIC_SUPABASE_URL=${{ secrets.NEXT_PUBLIC_SUPABASE_URL }} \
            --build-arg NEXT_PUBLIC_SUPABASE_ANON_KEY=${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }} \
            --build-arg NEXT_PUBLIC_API_URL=https://capvista-api.azurewebsites.net \
            -t capvistaregistry.azurecr.io/capvista-web:${{ github.sha }} \
            -t capvistaregistry.azurecr.io/capvista-web:latest .
          docker push capvistaregistry.azurecr.io/capvista-web:${{ github.sha }}
          docker push capvistaregistry.azurecr.io/capvista-web:latest

      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v3
        with:
          app-name: capvista-web
          publish-profile: ${{ secrets.AZURE_WEB_PUBLISH_PROFILE }}
          images: capvistaregistry.azurecr.io/capvista-web:${{ github.sha }}
